meta {
  name: ChildCreate
  type: http
  seq: 15
}

post {
  url: {{host}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "nom": "Child Tester",
    "email": "{{child_email}}",
    "password": "Password123!",
    "role": "ENFANT"
  }
}

script:pre-request {
  const randomId = Math.floor(Math.random() * 100000);
  bru.setVar("child_email", `child.test.${randomId}@cipestudio.com`);
}

script:post-response {
  test("Status code is 200", function() {
      expect(res.getStatus()).to.equal(200);
  });
}
