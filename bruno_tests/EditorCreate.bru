meta {
  name: EditorCreate
  type: http
  seq: 19
}

post {
  url: {{host}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "nom": "Editor Tester",
    "email": "{{editor_email}}",
    "password": "Password123!",
    "role": "EDITEUR"
  }
}

script:pre-request {
  const randomId = Math.floor(Math.random() * 100000);
  bru.setVar("editor_email", `editor.test.${randomId}@cipestudio.com`);
}

script:post-response {
  test("Status code is 200", function() {
      expect(res.getStatus()).to.equal(200);
  });
}
